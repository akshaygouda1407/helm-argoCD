apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: service-a-qa
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/akshaygouda1407/helm-argoCD.git
    targetRevision: main
    path: helm/service-a
    helm:
      valueFiles:
        - values-qa.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: qa

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true