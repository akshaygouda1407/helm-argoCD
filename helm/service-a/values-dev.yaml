replicaCount: 1

image:
  repository: akshaygouda646/service-a
  tag: v1
  pullPolicy: IfNotPresent

ingress:
  enabled: true
  host: service-a-dev.local

config:
  SPRING_PROFILES_ACTIVE: dev
  SERVICE_NAME: service-a

resources:
  requests:
    memory: 128Mi
    cpu: 50m
  limits:
    memory: 256Mi
    cpu: 250m

secret:
  create: false
  name: service-a-dev-secret
