apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "service-a.fullname" . }}-config
  labels:
    {{- include "service-a.labels" . | nindent 4 }}

data:
  application-name: {{ .Values.config.applicationName | quote }}
  spring-profile: {{ .Values.config.springProfile | quote }}