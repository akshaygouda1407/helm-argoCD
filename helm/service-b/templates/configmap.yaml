apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "service-b.fullname" . }}-config
  labels:
    {{- include "service-b.labels" . | nindent 4 }}
data:
  SPRING_PROFILES_ACTIVE: {{ .Values.config.SPRING_PROFILES_ACTIVE | quote }}
  SERVICE_NAME: {{ .Values.config.SERVICE_NAME | quote }}

